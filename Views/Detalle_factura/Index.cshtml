@model IEnumerable<AppSenaJmoc.Models.Detalle_factura>

@{
    ViewData["Title"] = "Crear Detalle Factura";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Crear Detalle Factura</h1>

<p>
    <a  class="btn btn-success" asp-action="Create">Crear Detalle Factura</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad_Detalle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Materiales)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad_Detalle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Materiales.Descripcion_materiales)
            </td>
            <td>
                <a  class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id_Detalle_factura">Editar</a> |
                <a  class="btn btn-info" asp-action="Details" asp-route-id="@item.Id_Detalle_factura">Informacion</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id_Detalle_factura">Borrar</a>
            </td>
        </tr>
}
    </tbody>
</table>
